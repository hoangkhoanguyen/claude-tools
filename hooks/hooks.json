{
  "//": "SDLC plugin hooks. SessionStart: if the project has a .sdlc/state.md, print it as a reminder of the work in progress (supports resuming). Read-only, changes nothing.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/sdlc-session-start.sh\""
          }
        ]
      }
    ]
  }
}
